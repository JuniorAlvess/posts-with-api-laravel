import Form from '../../components/Form';

const Register = () => {
    return (
        <Form 
            title="Cadastre-se"
            navigation="login"
            buttonText="Cadastrar"
        />
    );
}

export default Register;